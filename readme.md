This python script processes a `.bib` (BibTeX) file by converting the titles of entries to title case. It outputs an updated `.bib` file with the modified titles.

For example usage, see the included `bib.bib` file.
